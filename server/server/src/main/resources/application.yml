spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:mem:tidsbanken
    driverClassName: org.h2.Driver
    username: sa
  cloud:
    appId:
    gcp:
      sql:
        instance-connection-name:
        database-name:
          
server:
  port: 3400

